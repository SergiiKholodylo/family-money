<ContentDialog
    x:Class="FamilyMoney.UWP.Views.AddAccount"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FamilyMoney.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Uid="AddAccountDialogUid"
    mc:Ignorable="d"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick">

    <Grid>
        <StackPanel>
            <TextBox  
                x:Uid="AddAccountAccountNameUid"
                Text="{x:Bind ViewModel.Name, Mode=TwoWay}"/>
            <TextBox  
                x:Uid="AddAccountAccountDescriptionUid"
                Text="{x:Bind ViewModel.Description, Mode=TwoWay}"/>
            <TextBox  
                x:Uid="AddAccountAccountCurrencyUid"
                Text="{x:Bind ViewModel.Currency, Mode=TwoWay}"/>
        </StackPanel>
    </Grid>
</ContentDialog>
