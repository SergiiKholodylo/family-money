<Page
    x:Class="FamilyMoney.UWP.Views.Categories"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FamilyMoney.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:bases="using:FamilyMoneyLib.NetStandard.Bases"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Label="New Category" Icon="Add" Click="AppBarButton_Click"/>

            <AppBarSeparator />
            <AppBarButton Label="Transactions" Icon="Calculator" Click="AppBarButton_Click_1" />
            <AppBarButton Label="Accounts" Icon="Folder" />
            <AppBarButton Label="Categories" Icon="Bookmarks" />
            <AppBarButton Label="Report" Icon="ViewAll" />

            <CommandBar.SecondaryCommands>
                <AppBarButton Label="Create an account"  />
                <AppBarButton Label="Create a category" />
            </CommandBar.SecondaryCommands>

            <CommandBar.Content>
                <TextBlock Text="Main account - 205 UAH" />
            </CommandBar.Content>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid Padding="20">
        <StackPanel>
            <ListView ItemsSource="{x:Bind ViewModel.Categories}" HorizontalAlignment="Stretch" Header="Categories List">

                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="bases:Category">
                        <Grid HorizontalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="300"/>
                                <ColumnDefinition Width="60"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{x:Bind Name}" />
                            <TextBlock Grid.Column="1" Text="{x:Bind Description}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>
