<Page
    x:Class="FamilyMoney.UWP.Views.Accounts"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FamilyMoney.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:bases="using:FamilyMoneyLib.NetStandard.Bases"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Label="New Account" Icon="Add"  Click="AppBarButton_Click"/>

            <AppBarSeparator />
            <AppBarButton Label="Transactions" Icon="Calculator" Click="AppBarButton_Click_1" />
            <AppBarButton Label="Accounts" Icon="Folder" />
            <AppBarButton Label="Categories" Icon="Bookmarks" />
            <AppBarButton Label="Report" Icon="ViewAll" />

            <CommandBar.Content>
                <TextBlock Text="Main account - 205 UAH" />
            </CommandBar.Content>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid Padding="20">
        <StackPanel>
                <ListView ItemsSource="{x:Bind ViewModel.Accounts}" HorizontalAlignment="Stretch" Header="Account List">

                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="bases:Account">
                        <Grid HorizontalAlignment="Stretch">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="300"/>
                                <ColumnDefinition Width="60"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{x:Bind Name}" />
                            <TextBlock Grid.Column="1" Text="{x:Bind Description}" />
                            <TextBlock Grid.Column="2" Text="{x:Bind Currency}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>
