<Page
    x:Class="FamilyMoney.UWP.Views.Transactions"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FamilyMoney.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Label="New Transaction" Icon="Add" />

            <AppBarSeparator />
            <AppBarButton Label="Transactions" Icon="Calculator" Click="AppBarButton_Click" />
            <AppBarButton Label="Accounts" Icon="Folder" />
            <AppBarButton Label="Categories" Icon="Bookmarks" />
            <AppBarButton Label="Report" Icon="ViewAll" />

            <CommandBar.SecondaryCommands>
                <AppBarButton Label="Create an account" />
                <AppBarButton Label="Create a category" />
            </CommandBar.SecondaryCommands>

            <CommandBar.Content>
                <TextBlock Text="Main account - 205 UAH" />
            </CommandBar.Content>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid>
        <StackPanel>
            <ComboBox Header="Account" x:Name="cbAccount" ItemsSource="{x:Bind ViewModel.Accounts}" HorizontalAlignment="Stretch"/>
            <ListView Header="Transactions" x:Name="lvTransactions" ItemsSource="{x:Bind ViewModel.Transactions}" BorderBrush="Black" BorderThickness="1,1,1,1"/>
        </StackPanel>
    </Grid>
</Page>
